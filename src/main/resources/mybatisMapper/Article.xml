<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="Article">
    <insert id="insertArticle" parameterType="com.elkaly.blog.model.Article">
        <selectKey order="AFTER" keyProperty="articleNo" resultType="int">
            select last_insert_id()
        </selectKey>
        INSERT into articles ( title, contents)
        values (
        #{title},
        #{contents}
        )
    </insert>

    <select id="selectArticle" parameterType="Integer" resultType="com.elkaly.blog.model.Article">
        SELECT ARTICLE_NO as articleNo,
               TITLE,
               CONTENTS
          FROM ARTICLES
         WHERE ARTICLE_NO = #{articleNo}
    </select>

    <update id="updateArticle" parameterType="com.elkaly.blog.model.Article">
        UPDATE ARTICLES
           SET TITLE = #{title}
               , CONTENTS = #{contents}
         WHERE ARTICLE_NO = #{articleNo}
    </update>

    <delete id="deleteArticle" parameterType="Integer">
        DELETE FROM ARTICLES WHERE ARTICLE_NO = #{articleNo}
    </delete>

    <select id = "selectArticles" resultType="com.elkaly.blog.model.Article">
        SELECT ARTICLE_NO as articleNo,
               TITLE,
               CONTENTS
          FROM ARTICLES
    </select>
</mapper>